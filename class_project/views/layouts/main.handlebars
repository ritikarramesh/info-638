<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>{{title}}</title>
    <link rel='stylesheet' href='/bootstrap/css/bootstrap.css' />
  </head>
  <body data-bs-theme="dark">
    {{#if flash}}
      <div class="alert alert-dismissible fade show alert-{{flash.type}}" role="alert">
        <strong>{{flash.intro}}</strong> {{{flash.message}}}
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
      </div>
    {{/if}}
     <nav class="navbar navbar-expand-lg bg-body-tertiary">
      <div class="container-fluid">
        <a class="navbar-brand" href="/">BookedIn</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
          <div class="navbar-nav me-auto mb-2 mb-lg-0">
            <a class="nav-link" href="/genres">Genres</a>
            <a class="nav-link" href="/authors">Authors</a>
            <a class="nav-link" href="/books">Books</a>
          </div>
          <div class="d-flex">
            <div class="navbar-nav me-auto mb-2 mb-lg-0">
              {{#if currentUser}}
                <a class="nav-link" href="/users/profile">Welcome {{currentUser.name}}</a>
                <form action="/users/logout" role="form" method="post" class="d-flex">
                  <input type="hidden" name="_csrf" value="{{_csrfToken}}">
                  <button class="btn btn-outline-success" type="submit">Logout</button>
                </form>
              {{else}}
                <a class="nav-link" href="/users/register">Register</a>
                <a class="nav-link" href="/users/login">Log in</a>
              {{/if}}
            </div>
          </div>
        </div>
      </div>
    </nav>
    <div class="container">
      {{{body}}}
    </div>
    <script src="/bootstrap/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
